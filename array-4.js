const arr = ['метод', 'массив', 'цикл', 'фор', 'вайл', 'тру', 'фолс', 'константа', 'строка', 'объект']
let i = 0;
while (i < arr.length) {
    if (arr[i].length > 3) {
        console.log(arr[i])
    }
    i++;
} 